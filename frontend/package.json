{
  "name": "pega-texto-frontend",
  "private": true,
  "type": "module",
  "version": "1.3.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "cap:sync": "npm run build && npx cap sync",
    "android:add": "npx cap add android",
    "android:sync": "npm run build && npx cap sync android && npm run android:brand",
    "android:open": "npx cap open android",
    "android:run": "npm run build && npx cap sync android && npm run android:brand && npx cap run android",
    "android:apk": "npm run build && npx cap sync android && npm run android:brand && cd android && ./gradlew assembleDebug",
    "test": "node --test test/*.test.mjs",
    "android:brand": "node scripts/apply-android-customizations.mjs"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/clipboard": "^8.0.1",
    "@capacitor/core": "^8.5.0",
    "@mozilla/readability": "^0.6.0",
    "marked": "^18.0.10",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "@vitejs/plugin-react": "^4.3.1",
    "autoprefixer": "^10.4.19",
    "postcss": "^8.4.38",
    "tailwindcss": "^3.4.4",
    "vite": "^5.3.1"
  }
}
